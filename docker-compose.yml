version: '3'

services:
  producer:
    build: 
      context: ./Producer
    ports:
      - "8000:8000"
    depends_on:
      - rabbitmq

  consumer_one:
    build: 
      context: ./Consumer_one
    ports:
      - "8001:8001"
    depends_on:
      - rabbitmq

  consumer_two:
    build: 
      context: ./Consumer_two
    ports:
      - "8002:8002"
    depends_on:
      - rabbitmq

  consumer_three:
    build: 
      context: ./Consumer_three
    ports:
      - "8003:8003"
    depends_on:
      - rabbitmq

  consumer_four:
    build: 
      context: ./Consumer_four
    ports:
      - "8004:8004"
    depends_on:
      - rabbitmq

  rabbitmq:
    image: "rabbitmq:3-management"
    ports:
      - "5672:5672"
      - "15672:15672"

  database:
    image: "mysql:5.7"
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=cts112233
      - MYSQL_DATABASE=inventory_management